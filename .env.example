NEXT_PUBLIC_SUPABASE_ANON_KEY="" # Supabase anon key (client-safe)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="" # Supabase publishable key (client-safe)
NEXT_PUBLIC_SUPABASE_URL="" # Supabase project URL
POSTGRES_DATABASE="postgres" # Default database name
POSTGRES_HOST="" # Supabase database host
POSTGRES_PASSWORD="" # Database password
POSTGRES_PRISMA_URL="" # Prisma connection string (pooler)
POSTGRES_URL="" # Database connection string (pooler)
POSTGRES_URL_NON_POOLING="" # Database connection string (direct)
POSTGRES_USER="postgres" # Database user
SUPABASE_ANON_KEY="" # Supabase anon key (server-side use)
SUPABASE_JWT_SECRET="" # Supabase JWT secret
SUPABASE_PUBLISHABLE_KEY="" # Supabase publishable key (server-side use)
SUPABASE_SECRET_KEY="" # Supabase secret key
SUPABASE_SERVICE_ROLE_KEY="" # Supabase service role key (admin server-side only)
SUPABASE_URL="" # Supabase project URL (server-side use)

R2_ACCOUNT_ID="" # Cloudflare R2 account ID
R2_ACCESS_KEY_ID="" # Cloudflare R2 access key
R2_SECRET_ACCESS_KEY="" # Cloudflare R2 secret key
R2_BUCKET_NAME="" # R2 bucket name
R2_PUBLIC_URL="" # Public base URL for R2 assets


RISA_API_URL= ""